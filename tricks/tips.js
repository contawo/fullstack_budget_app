import {Button, Elements, Document1} from "./tricks.js"

// console.log(name())

const elements = new Elements()


document.body.appendChild(Button("hello", "btn"))
document.body.appendChild(elements.createButton())
// document.body.appendChild(ele)
